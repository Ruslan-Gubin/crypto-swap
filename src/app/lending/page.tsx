import { CONFIG_APP } from "@/shared";

export const metadata = CONFIG_APP.META_TAGS.lending;

export default async function Lending() {
  return (
    <section >
      <div>Lending page</div>
    </section>
  );
}

