import { CONFIG_APP } from "@/shared";

export const metadata = CONFIG_APP.META_TAGS.feedback;

export default async function Feedback() {
  return (
    <section >
      <div>Feedback page</div>
    </section>
  );
}